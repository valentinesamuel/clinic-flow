---
paths:
  - "."  # Only loads when you are working in this folder
  #- "src/frontend/**/*"  # Only loads when you are working in this folder
---
# React Vite Standards
- Use functional components with TypeScript interfaces.
- Components go in `src/components/ui` for primitives.